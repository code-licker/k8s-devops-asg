apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-custom
spec:
  serviceName: "postgres-custom-service"
  replicas: 1
  selector:
    matchLabels:
      app: postgres-custom-db
  template:
    metadata:
      labels:
        app: postgres-custom-db
    spec:
      containers:
        - name: postgres
          image: ghcr.io/code-licker/nagp-2026-anime-db:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
              name: dbport
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-db-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-db-secrets
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: postgres-db-secrets
                  key: POSTGRES_DB
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: postgres-data-volume
              mountPath: /var/lib/postgresql/data 
      volumes:
        - name: postgres-data-volume
          persistentVolumeClaim:
            claimName: postgres-custom-pvc 